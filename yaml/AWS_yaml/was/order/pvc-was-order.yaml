apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-was-order
  namespace: was
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: sc-was-order
  resources:
    requests:
      storage: 2Gi